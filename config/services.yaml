parameters:
    # Configuration pour l'upload de fichiers CSC
    csc_upload_directory: '%kernel.project_dir%/public/uploads/csc'
    csc_max_file_size: 10485760  # 10MB en octets
    csc_allowed_extensions: ['pdf', 'jpg', 'jpeg', 'png', 'gif']
    
    # Configuration Webservice Commando
    commando_url_base: 'http://192.168.51.44//COMMANDO_WEB_SERVICE_WEB/FR/cdo_get.awp'
    commando_cle_privee: 'lfLidEMDKdd45s85c2d5'
    commando_default_client: '00318945'  # Code client par défaut pour les tests

services:
    # Default configuration for services in *this* file
    _defaults:
        # Automatically injects dependencies in your services
        autowire: true

        # Automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true

        # Allows optimizing the container by removing unused services; this also means
        # fetching services directly from the container via $container->get() won't work
        public: false

    _instanceof:
        Sylius\Bundle\ResourceBundle\Controller\ResourceController:
            autowire: false
        Sylius\Bundle\ResourceBundle\Form\Type\AbstractResourceType:
            autowire: false

    # Makes classes in src/ available to be used as services;
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/*'
        exclude: '../src/{Entity,Kernel.php}'

    # Controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller'
        tags: ['controller.service_arguments']

    # Configuration du service Commando API
    App\Service\CommandoApiService:
        arguments:
            $commandoUrlBase: '%commando_url_base%'
            $commandoClePrivee: '%commando_cle_privee%'
